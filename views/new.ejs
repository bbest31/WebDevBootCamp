<% include partials/header %>
<div class="ui main text container segment">
    <div class="ui huge header">New Marine</div>
    <form class="ui form" action="/space_marines" method="POST">
        <div class="field">
            <label>Title</label>
            <input type="text" name="marine[title]" placeholder="soldier title">
        </div>
        <div class="field">
            <label>Image</label>
            <input type="text" name="marine[image]" placeholder="soldier image">
        </div>
        <div class="field">
            <label>Bio</label>
            <textarea name="marine[body]"></textarea>
            </div>
        <button class="ui black basic button">Submit</button>
    </form>
</div>
<% include partials/footer %>